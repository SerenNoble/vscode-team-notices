# 帮助手册（常见问题）

> 按问题分类查找，用页面顶部搜索框输入关键字即可过滤章节。没有找到答案？请用侧边栏「❌ 问题反馈」提交。

## 安装与加载

### 插件安装后侧边栏没有出现？

- 确认 VSCode 内核版本 ≥ 1.70（帮助 → 关于）。
- 查看活动栏是否被折叠：右键活动栏，确认「AIGC 工具箱」已勾选。
- 重载窗口：`Ctrl+Shift+P` → `Developer: Reload Window`。

### 反馈表单打开是白屏？

已知限制：部分定制版桌面 VSCode 不支持带脚本的 Webview（`$loadForeignModule` 报错）。
浏览器型 WebIDE 不受影响。桌面版遇此问题请改用 WebIDE，或在浏览器里完成反馈。

## 模型使用

### 不知道选哪个模型？

- 日常任务：`auto-model-standard`（默认推荐，能力与速度均衡）。
- 复杂推理、要质量：`auto-model-pro`（较强但较慢）。
- 简单快速任务：`auto-model-fast`（最快）。
- 要识别图片：`auto-model-vl`。
- 详见「🤖 模型选择和介绍」页面。

### 提示模型名已弃用 / 连接失败？

`comware-model`、`comware-model-air`、`claude-sonnet-4-6`、`claude-opus-4-6`
已弃用并将逐步下线，请切换到 `auto-model-standard` / `auto-model-pro` / `auto-model-fast`。

### API Key 怎么获取？

红区用户访问 http://10.113.36.131:8081/newapi/query/ ，输入域账号即可查询自己的 Key。

### Claude Code 怎么切换模型？

- 临时：会话内输入 `/model` 选择模型，仅当前对话生效。
- 持久（推荐）：编辑 `~/project/.claude/settings.local.json`，在 env 中设置
  `ANTHROPIC_MODEL` 等环境变量后重启，完整示例见「🤖 模型选择和介绍」页面。

## 项目配置

### 怎么修改绑定的项目流水号（NVID）？

需要在 **两个扩展** 里同时修改：

1. 打开 WebIDE 左侧「扩展」面板
2. 找到 **CmwCoderAsst** → 点击设置（齿轮）→ 填入新的项目流水号
3. 再找到 **h3c-vscode-all-in-one** → 同样点击设置 → 也填入新的项目流水号

> 两个扩展都要改，只改一个不生效。

## 问题反馈

### 提交提示「疑似涉密/敏感信息，已阻止提交」？

提交前会自动做涉密检查。删除描述中的疑似敏感内容（内网地址、账号、密钥、客户名等）
后重试；如属误判，请调整描述方式（用泛化表述代替具体值）。

### 提示「已连续提交带代码的问题反馈」？

连续提交含代码的反馈会被限制，请改用文字描述问题（报错关键字、现象说明）。

### 提交失败 / 一直转圈？

- 提交走 Jenkins（`cmwdevops.h3c.com:8080`），先确认能否访问该地址。
- 网络闪动导致超时可稍后重试；持续失败请按下面「反馈渠道」上报。

## 通知公告

### 为什么公告有角标但点「忽略」后不再弹窗？

同一条公告只弹窗提醒一次（避免打扰），角标会一直保留，直到你点开「📢 通知公告」页面。

### 公告不是最新的？

- 公告每 2 分钟自动同步一次，刚发布的内容稍等片刻。
- 打开公告页会立即拉取最新内容。
- 网络不通时显示上次缓存或内置内容，页面会标注「远端拉取失败」。

## 反馈渠道

以上都没解决？请用侧边栏「❌ 问题反馈」提交，类型选「咨询」或「故障」，
描述里写清：使用场景、报错信息关键字、复现步骤，会有人跟进处理。
